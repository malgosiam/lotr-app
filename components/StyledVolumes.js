import styled from "styled-components";

const StyledVolumes = styled.ul`
  display: flex;
  flex-directon: row;
  list-style: none;
  gap: 1.5rem;
  margin: 0;
  padding: 0;
`;

export default StyledVolumes;
